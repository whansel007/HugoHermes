# HugoHermes
Scripts Hermes agent runs to save on tokens and ensure a deterministic format.
The folder currently contains:
    1. get_emails.py and the correlating shell script
        Run and send the final stdout to the user in telegram
